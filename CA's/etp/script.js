const password = document.getElementById('password').value;
const button=document.querySelector('button')
    function handleLogin() {
    if (password<=8) {
      document.write(alert('Password must be in 8 digits.'));
    }else{
      function 

    }
}